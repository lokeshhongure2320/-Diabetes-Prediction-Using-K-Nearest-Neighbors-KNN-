# -Diabetes-Prediction-Using-K-Nearest-Neighbors-KNN-
This project aims to predict whether a patient has diabetes using the K-Nearest Neighbors (KNN) algorithm, a supervised machine learning method used for classification problems.  The model classifies a patient as diabetic or non-diabetic based on the similarity (distance) to nearby data points.
